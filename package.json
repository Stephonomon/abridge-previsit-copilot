{
  "name": "previsit-copilot",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "card:walter": "npm run card:walter --workspace server"
  },
  "workspaces": [
    "server",
    "web"
  ]
}
